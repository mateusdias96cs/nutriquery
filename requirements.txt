# NutriQuery — dependências diretas, em Python 3.12.
# Pinadas nas versões com que o projeto foi construído e testado.
# Só o que o projeto realmente usa; as transitivas o pip resolve.

# Warehouse
duckdb==1.5.4

# Ingestão bronze + eval
pandas==3.0.3
numpy==2.5.0
openpyxl==3.1.5      # engine que o pd.read_excel usa para ler data/raw/taco.xlsx
pyarrow==24.0.0      # parquet dos resultados de referência em evals/gold_results/

# Transformação (silver → gold)
dbt-core==1.11.11
dbt-duckdb==1.10.1

# Orquestração
dagster==1.13.10
dagster-dbt==0.29.10
dagster-webserver==1.13.10   # serve a UI do `dagster dev`

# Agente
langgraph==1.2.6
langchain-core==1.4.8
langchain-groq==1.1.3
langgraph-checkpoint-sqlite==3.1.0   # persiste as conversas em db/checkpoints.sqlite
